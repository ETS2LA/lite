Added the backend for the communication between plugins and the main app
Plugins are now disabling themselves when they crash
The crash reporter will catch every crash now
Improved the PyTorch module
Fixed context menu behavior
(Currently some plugins are not working)
Plugins can now communicate with each other and the main process using the shared memory directly instead of using the queues
Added the new mapping code temporarily to the ACC plugins
Fixed development mode
Small fixes in the crash reporter and the main process